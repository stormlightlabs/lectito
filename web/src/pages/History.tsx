import { PageShell } from "./PageShell";

export function HistoryPage() {
  return (
    <PageShell eyebrow="History" title="Run history">
      <p>Review saved URL and HTML extraction runs.</p>
    </PageShell>
  );
}
