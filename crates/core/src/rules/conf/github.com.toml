name = "github"
hosts = ["github.com"]
content_roots = ["//article[contains(@class, 'markdown-body')]"]
remove = ["//div[contains(@class, 'Header')]"]

[metadata]
title = ["//h1"]

[fallback]
generic_on_empty = true
