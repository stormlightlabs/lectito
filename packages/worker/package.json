{
  "name": "lectito-worker",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "dry-run": "wrangler deploy --dry-run",
    "format": "pnpm -w run format -- packages/worker",
    "typecheck": "tsc --noEmit",
    "types": "wrangler types"
  },
  "dependencies": { "hono": "^4.10.7" },
  "devDependencies": { "typescript": "~6.0.2", "wrangler": "^4.105.0" }
}
