# Cloudflare Pages serves real static files first; only paths that don't
# match a file fall through to these rules. `/api/*` is owned by the edge
# worker proxy and never reaches Pages routing.

/docs /docs/ 301
/* /index.html 200
